server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Izinkan upload file sampai 20MB
    client_max_body_size 20M;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/javascript;

    # Proxy /api/ ke backend — harus di atas block regex
    location /api/ {
        proxy_pass              http://backend:5000;
        proxy_http_version      1.1;
        proxy_set_header        Host              $host;
        proxy_set_header        X-Real-IP         $remote_addr;
        proxy_set_header        X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_read_timeout      120s;
        proxy_send_timeout      120s;
        proxy_connect_timeout   10s;
        proxy_request_buffering off;
    }

    # Proxy /uploads/ ke backend — pakai ^~ agar menang dari regex
    location ^~ /uploads/ {
        proxy_pass         http://backend:5000;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_read_timeout 60s;
    }

    # Cache JS/CSS (tidak bentrok dengan uploads)
    location ~* \.(js|css|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # Cache gambar STATIS (di dalam /usr/share/nginx/html saja)
    # /uploads/ sudah ditangani di atas, tidak akan masuk sini
    location ~* \.(png|jpg|jpeg|gif|svg|ico)$ {
        expires 1h;
        add_header Cache-Control "public";
        try_files $uri =404;
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
